# Codecov YAML
# https://docs.codecov.com/docs/codecov-yaml

codecov:
  branch: main

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
