[project]
name = "reactor"
version = "0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=2.3.0",
  "django>=5.2.1",
  "python-decouple>=3.8",
]

[dependency-groups]
dev = [
  "model-bakery>=1.20.4",
  "pytest>=8.3.5",
  "pytest-cov>=6.1.1",
  "pytest-django>=4.11.1",
  "pytest-mock>=3.14.0",
  "pytest-randomly>=3.16.0",
  "ruff>=0.11.11",
]
