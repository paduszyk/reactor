[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=2.3.0",
  "django>=5.1.4",
]

[project.optional-dependencies]
postgres = [
  "psycopg[binary]>=3.2.3",
]

[dependency-groups]
local = [
  "pre-commit>=4.0.1",
]
ruff = [
  "python-dotenv>=1.0.1",
  "ruff>=0.8.4",
]
