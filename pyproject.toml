[project]
name = "reactor"
version = "1-dev"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=2.3.0",
  "django>=5.2",
  "psycopg[binary]>=3.2.6",
  "python-decouple>=3.8",
]

[dependency-groups]
dev = [
  "ruff>=0.11.6",
]
