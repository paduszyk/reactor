[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dj-database-url>=2.3.0",
    "django>=5.1.4",
]

[dependency-groups]
dev = [
  "pre-commit>=4.0.1",
  "pytest>=8.3.4",
  "pytest-django>=4.9.0",
  "pytest-mock>=3.14.0",
  "pytest-randomly>=3.16.0",
  "python-dotenv>=1.0.1",
  "ruff>=0.8.3",
]
