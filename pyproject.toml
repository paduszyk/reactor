[project]
name = "reactor"
version = "1-alpha.0"
requires-python = ">=3.13"
dependencies = [
  "django>=5.2.1",
  "environs[django]>=14.1.1",
  "psycopg[binary]>=3.2.9",
]

[dependency-groups]
dev = [
  "django-debug-toolbar>=5.2.0",
  "ruff>=0.11.10",
]
