[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=2.3.0",
  "django>=5.2",
]

[dependency-groups]
dev = [
  "model-bakery>=1.20.4",
  "pytest-cov>=6.1.1",
  "pytest-django>=4.11.1",
  "pytest>=8.3.5",
  "python-dotenv>=1.1.0",
  "ruff>=0.11.5",
]
