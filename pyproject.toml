[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "django>=5.1.7",
  "environs[django]>=14.1.1",
  "psycopg[binary]>=3.2.5",
]

[dependency-groups]
ruff = [
  "ruff>=0.9.10",
]
