[project]
name = "reactor"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
  "django>=5.1.6",
  "django-environ>=0.12.0",
  "psycopg[binary]>=3.2.5",
]

[dependency-groups]
debug = [
  "django-debug-toolbar>=5.0.1",
]
ruff = [
  "ruff>=0.9.9",
]
