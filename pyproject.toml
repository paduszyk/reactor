[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=2.3.0",
  "django>=5.2",
]

[dependency-groups]
dev = [
  "python-dotenv>=1.1.0",
  "ruff>=0.11.5",
]
