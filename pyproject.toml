[project]
name = "reactor"
version = "1-alpha.0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=3.0.1",
  "django>=5.2.5",
  "pillow>=11.3.0",
  "psycopg[binary]>=3.2.9",
  "python-decouple>=3.8",
]

[dependency-groups]
ruff = [
  "ruff>=0.12.8",
]
