[project]
name = "reactor"
version = "1-dev"
requires-python = ">=3.13"
dependencies = [
  "django>=5.2.6,<6",
]

[dependency-groups]
dev = [
  "pytest>=8.4.2,<9",
  "pytest-cov>=6.3.0,<7",
  "pytest-django>=4.11.1,<5",
  "pytest-mock>=3.15.0,<4",
  "pytest-randomly>=3.16.0,<4",
  "ruff>=0.12.12,<0.13",
]
