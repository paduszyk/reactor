[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dj-database-url>=2.3.0",
    "django>=5.1.4",
]

[dependency-groups]
dev = [
  "pre-commit>=4.0.1",
  "ruff>=0.8.3",
]
