[project]
name = "reactor"
version = "0.1-alpha.0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=3.0.0",
  "django>=5.2.3",
  "python-decouple>=3.8",
]

[dependency-groups]
dev = [
  "ruff>=0.11.13",
]
