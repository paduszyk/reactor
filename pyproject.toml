[project]
name = "reactor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "dj-database-url>=2.3.0",
  "django>=5.1.7",
  "psycopg[binary]>=3.2.6",
  "python-decouple>=3.8",
]

[dependency-groups]
ruff = [
  "ruff>=0.11.2",
]
