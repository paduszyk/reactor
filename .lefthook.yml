templates:
  npm-exec: npm exec --no --

pre-commit:
  jobs:
    - name: Prettier
      run: |
        {npm-exec} prettier --check .
    - name: uv
      group:
        jobs:
          - name: Lock file check
            run: |
              uv lock --check
