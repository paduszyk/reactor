pre-commit:
  jobs:
    - name: prettier
      run: npx --no-install prettier --check .
    - name: uv
      run: uv lock --check

commit-msg:
  only:
    - ref: main
  jobs:
    - name: commitlint
      run: npx --no-install commitlint --edit {1}

output:
  - meta
  - summary
