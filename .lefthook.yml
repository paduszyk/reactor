commit-msg:
  jobs:
    - name: commitlint
      only:
        - ref: main
      run: npx commitlint --edit {1}

pre-commit:
  jobs:
    - name: prettier
      run: npx prettier --check .

output:
  - meta
  - summary
