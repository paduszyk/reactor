pre-commit:
  jobs:
    - name: prettier
      run: npx prettier --check .
    - name: uv
      run: uv lock --check
    - name: ruff (linter)
      run: uv run ruff check --output-format=full .
    - name: ruff (formatter)
      run: uv run ruff format --diff .

output:
  - meta
  - summary
